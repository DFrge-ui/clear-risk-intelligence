# Optional configuration. This file is documentation, not loaded automatically.
# Set these in the environment of the process serving Flask.
# CLEAR_SECRET_KEY=<a long random value; never commit a real secret>
# CLEAR_HTTPS=1  (only when the app is actually served over HTTPS)
